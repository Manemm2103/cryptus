{
  "name": "cryptus",
  "version": "0.1.0",
  "description": "Two-person private chat with per-user passwords, read redaction, emoji support, and media messages.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
